// Este archivo se ejecuta automáticamente antes de cada prueba.
// Importa los matchers personalizados de jest-dom para poder usar:
//   expect(...).toBeInTheDocument()
//   expect(...).toHaveValue()
//   expect(...).toBeVisible()
//   etc.
import "@testing-library/jest-dom";
